# @package recall
name: popularity
type: heuristic

model:
  time_decay: false
  decay_half_life_days: 30        # used when time_decay is true

output:
  top_k: 200

eval:
  k_list: [10, 50, 100, 200]
